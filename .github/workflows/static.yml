# This workflow was replaced by pages.yml which targets the main branch only.
# Kept as a placeholder to avoid broken references. Do not use.
#
# To deploy: push to main (pages.yml handles it automatically)
# or use: Actions → "Deploy to GitHub Pages" → Run workflow
